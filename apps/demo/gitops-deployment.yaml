apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo
  namespace: demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-container
  template:
    metadata:
      labels:
        app: demo-container
    spec:
      containers:
        - name: demo-container
          image: docker.io/hongsoonchan/demo:58cccac
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: demo-config